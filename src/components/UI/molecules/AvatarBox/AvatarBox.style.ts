import { styled } from "styled-components";

const AvatarBox = styled.div`
  grid-area: ava;
`;

const AvatarImg = styled.img`
  max-width: 20px;
`;

export const St = {
  AvatarBox,

  AvatarImg,
};
