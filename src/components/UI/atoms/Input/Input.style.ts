import styled from "styled-components";

const Input = styled.input`
  border: none;
  background: transparent;
  outline: none;
  color: var(--white);
  height: 10px;
  font-size: 14px;
  padding: 5px;
  width: 100%;
`;

export const St = {
  Input,
};
